## To test the module written for Chrome Extension using Node.js

1. Touch a package.json the same directory as the module.

```json
{
    "type": "module"
}
```

2. Run the following command directly in the terminal:

```zsh
node lookupIP.js
```
