{
  "name": "rule-updater",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "WWW_DIR=\"$(pwd)/../www\" node ./src/index.js",
    "start": "NODE_ENV=production WWW_DIR=\"$(pwd)/../www\" node ./src/index.js",
    "stop": "kill -9 $(lsof -t -i:3000) || echo 'No process running on port 3000'",
    "open": "open http://localhost:3000/shadowrocket",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage"
  },
  "main": "index.js",
  "keywords": [],
  "author": "Eugene Wang",
  "license": "ISC",
  "dependencies": {
    "@fastify/autoload": "^6.3.1",
    "@fastify/cors": "^11.1.0",
    "dotenv": "^17.2.3",
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vitest/ui": "^4.0.9",
    "vitest": "^4.0.9"
  }
}
