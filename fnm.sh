# brew install fnm
# Add to your shell (Zsh on macOS)
# echo 'eval "$(fnm env --use-on-cd)"' >> ~/.zshrc
# exec zsh

# Install and use Node 24 (latest LTS in the 24 line)
# fnm install 24
# fnm use 24

# verify
node -v
pnpm -v
